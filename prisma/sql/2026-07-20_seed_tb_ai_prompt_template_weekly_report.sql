-- ================================================================
-- 주간보고 초안 생성 — 시스템 공통 기본 프롬프트 seed
--   작성일 : 2026-07-20
--   전제   : tb_ai_prompt_template 이 이미 존재해야 함
--
--   멱등성 : ON CONFLICT DO NOTHING — 고정 UUID 이므로 재실행해도 중복 insert 안 됨.
--   default_yn = 'Y', prjct_id = NULL → 시스템 공통 기본값.
--            프로젝트가 자체 프롬프트를 원하면 같은 task_ty_code로 프로젝트 전용
--            행(prjct_id NOT NULL)을 추가하면 조회 시 이 공통 행보다 우선한다.
--
--   사용처 : src/app/api/projects/[id]/weekly-reports/route.ts (POST)
--            그 주 팀원 업무일지(TbWrWorkLog)를 모아 <업무일지 데이터> 로 붙인 뒤
--            이 시스템 프롬프트와 함께 TbAiTask.req_cn 으로 조립한다.
-- ================================================================

BEGIN;

INSERT INTO tb_ai_prompt_template (
  tmpl_id, prjct_id, tmpl_nm, task_ty_code, ref_ty_code,
  sys_prompt_cn, tmpl_dc,
  use_yn, default_yn, sort_ordr, creat_mber_id
) VALUES (
  '22222222-2222-2222-2222-000000000001', NULL,
  '주간보고 초안 (기본)', 'WEEKLY_REPORT_DRAFT', 'WEEKLY_REPORT',
  -- sys_prompt_cn
  '당신은 소프트웨어 개발 프로젝트의 PM을 돕는 보조 작성자입니다.
아래 <업무일지 데이터> 는 그 주(월~일) 팀원별 일일 업무일지(완료/미완료 항목, 메모)입니다.
이 데이터를 바탕으로 PM이 사내 주간보고에 그대로 옮겨 쓸 수 있는 초안을 한국어 마크다운으로 작성하세요.

출력 형식은 반드시 아래 두 섹션으로 구성합니다:

## 금주실적
- 팀원별로 묶지 말고, 이번 주에 실제로 완료된 작업을 업무 단위로 묶어 불릿으로 요약합니다.
- 사소한 반복 작업은 합쳐서 요약하고, 의미 있는 진척(기능 완료, 이슈 해결 등) 위주로 씁니다.
- 완료 항목이 없으면 "금주 완료된 항목이 없습니다." 라고 씁니다.

## 차주계획
- 아직 미완료인 항목과, 업무일지의 "이번주 계획"에서 다음 주로 넘어갈 것으로 보이는 항목을 불릿으로 정리합니다.
- 특이사항 메모에 리스크나 이슈가 언급되어 있으면 차주계획 하단에 "- 참고: ..." 형태로 짧게 덧붙입니다.

담백하고 간결한 보고체로 작성하고, 업무일지에 없는 내용은 추측해서 지어내지 마세요.',
  '주간보고 "초안 생성" 버튼 클릭 시 사용되는 시스템 공통 기본 프롬프트',
  'Y', 'Y', 0, NULL
)
ON CONFLICT (tmpl_id) DO NOTHING;

COMMIT;
