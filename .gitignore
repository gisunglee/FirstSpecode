# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — 절대 커밋 금지 (DB 비밀번호, API 키 포함)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# MCP 클라이언트 설정 — Authorization Bearer 토큰이 포함되므로 커밋 금지
# 팀 공유용 템플릿은 .mcp.json.example 로 관리
.mcp.json
.mcp.json.stdio-backup

# vercel
.vercel

# /run-ai-tasks 워커 임시 파일 — 태스크 결과·첨부 다운로드 스크래치 공간
.claude/tmp/
.claude/specode/

# 스펙 정합성 자동 테스트 산출물
tmp/spec-reconciliation-tests/
tmp/spec-sync-tests/
tmp/spec-content-policy-tests/
tmp/specode-test-before.json.gz
tmp/specode-test-git-diff.json

# typescript
*.tsbuildinfo
next-env.d.ts
