# UW-00012 프로젝트 설정 관리

> ## 1. 개요
| 항목 | 내용 |
|:-----|:-----|
| **단위업무ID** | UW-00012 |
| **단위업무명** | 프로젝트 설정 관리 |
| **비즈니스 목적** | OWNER/ADMIN이 프로젝트 기본 정보·AI API 키·AI 호출 방식을 한 곳에서 관리하고 설정 변경 이력을 확인한다. |
| **관련 요구사항** | RQ-00012, RQ-00008 |
| **기술 스택** | - |

## 2. 화면 목록
| 화면ID | 화면명 | URL | 유형 | 설명 |
|:-------|:-------|:----|:-----|:-----|
| PID-00023 | 프로젝트 설정 | /projects/{projectId}/settings | TAB | 기본정보·AI설정·변경이력 탭으로 구성 |

## 3. 화면 흐름
```
[프로젝트 메인] ──(설정 메뉴 클릭)──▶ [프로젝트 설정 - 기본정보 탭]
[기본정보 탭] ──(저장)──▶ [기본정보 탭 (갱신)]
[AI설정 탭] ──(API 키 등록·수정·삭제)──▶ [AI설정 탭 (갱신)]
[AI설정 탭] ──(호출 방식 변경)──▶ [AI설정 탭 (갱신)]
[변경이력 탭] ──(진입)──▶ [변경이력 목록 표시]
```

| 이동 | 전달 파라미터 | 동작 |
|:-----|:-------------|:-----|
| 프로젝트 메인 → 프로젝트 설정 | projectId | 설정 메뉴 클릭 |

## 4. 권한 정의
| 기능 | VIEWER | PM/DESIGNER/DEVELOPER | ADMIN | OWNER |
|:-----|:-------|:----------------------|:------|:------|
| 프로젝트 설정 접근 | ❌ | ❌ | ✅ | ✅ |
| 기본정보 수정 | ❌ | ❌ | ✅ | ✅ |
| AI API 키 관리 | ❌ | ❌ | ✅ | ✅ |
| AI 호출 방식 변경 | ❌ | ❌ | ✅ | ✅ |
| 변경이력 조회 | ❌ | ❌ | ✅ | ✅ |

## 5. 상태 정의
> 상태 전이가 없는 업무는 이 섹션을 생략합니다.

## 6. 참조 테이블
- <TABLE_SCRIPT:tb_pj_project>
- <TABLE_SCRIPT:tb_pj_project_api_key>
- <TABLE_SCRIPT:tb_pj_project_settings>
- <TABLE_SCRIPT:tb_pj_settings_history>

총 화면 1개 · 영역 4개 · 기능 9개

---

## PID-00023 프로젝트 설정

### AR-00032 탭 네비게이션 (TAB)

### 영역: [AR-00032] 탭 네비게이션

**유형:** TAB

**UI 구조**
```
+──────────────────────────────────────+
| [기본정보] [AI설정] [변경이력]        |
+──────────────────────────────────────+
```

**구성 항목**
| 항목명 | UI 타입 | 설명 | 기본값 |
|:-------|:--------|:-----|:-------|
| 기본정보 탭 | tab | 프로젝트명·발주처 수정 | 활성 |
| AI설정 탭 | tab | API 키 관리·호출 방식 선택 | 비활성 |
| 변경이력 탭 | tab | 설정 변경 이력 조회 | 비활성 |

#### FID-00074 탭 전환

#### 기능: [FID-00074] 탭 전환

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | BUTTON |
| **API** | - |
| **트리거** | 탭 클릭 시 |

**처리 로직**
```
1. 클릭한 탭 활성화
2. 해당 탭 콘텐츠 표시
3. 비활성 탭 콘텐츠 숨김
```

### AR-00033 기본정보 탭 (FORM)

### 영역: [AR-00033] 기본정보 탭

**유형:** FORM

**UI 구조**
```
+──────────────────────────────────────+
| 프로젝트명 * [___________________]   |
| 발주처      [___________________]   |
| 시작일      [날짜 선택]              |
| 종료일      [날짜 선택]              |
|                          [저장]     |
+──────────────────────────────────────+
```

**구성 항목**
| 항목명 | UI 타입 | 설명 | 기본값 |
|:-------|:--------|:-----|:-------|
| 프로젝트명 | text input | 필수 입력 | 현재값 |
| 발주처 | text input | 선택, 산출물 자동 생성 시 활용 | 현재값 |
| 시작일 | date picker | 선택 입력 | 현재값 |
| 종료일 | date picker | 선택, 시작일 이후여야 함 | 현재값 |
| 저장 | button (primary) | 유효성 검증 후 수정 API 호출 | - |

#### FID-00075 기본정보 조회

#### 기능: [FID-00075] 기본정보 조회

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | SELECT |
| **API** | `GET /api/projects/{projectId}` |
| **트리거** | 화면 진입 시 자동 실행 |

**Output**
| 필드 | 타입 | 설명 |
|:-----|:-----|:-----|
| name | string | 프로젝트명 |
| clientName | string | 발주처 |
| startDate | date | 시작일 |
| endDate | date | 종료일 |

**처리 로직**
```
1. 프로젝트 정보 API 호출
2. 서버 처리:
   a. tb_pj_project에서 prjct_id로 조회 (prjct_nm, client_nm, bgng_de, end_de)
3. 각 필드 바인딩
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project | prjct_nm | 프로젝트명 |
| tb_pj_project | client_nm | 발주처 |
| tb_pj_project | bgng_de | 시작일 |
| tb_pj_project | end_de | 종료일 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 권한 없음 | 403 | 접근 권한이 없습니다 |

#### FID-00076 기본정보 저장

#### 기능: [FID-00076] 기본정보 저장

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | UPDATE |
| **API** | `PUT /api/projects/{projectId}` |
| **트리거** | [저장] 버튼 클릭 시 |

**Input**
| 파라미터 | 타입 | 필수 | 설명 |
|:---------|:-----|:-----|:-----|
| name | string | Y | 프로젝트명 |
| clientName | string | N | 발주처 |
| startDate | date | N | 시작일 |
| endDate | date | N | 종료일 |

**처리 로직**
```
1. 프로젝트명 공백 검증
2. 종료일이 시작일 이전인지 검증
3. 수정 API 호출
4. 서버 처리:
   a. tb_pj_project UPDATE (prjct_nm, client_nm, bgng_de, end_de, mdfcn_dt = NOW())
   b. 변경된 항목별 tb_pj_settings_history INSERT:
      - chg_mber_id = 현재 회원
      - chg_item_nm = 변경 항목명 (예: '프로젝트명', '발주처')
      - bfr_val_cn = 변경 전 값
      - aftr_val_cn = 변경 후 값
5. 성공 시 '저장되었습니다' 토스트 표시
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project | prjct_nm | 프로젝트명 수정 |
| tb_pj_project | client_nm | 발주처 수정 |
| tb_pj_project | bgng_de | 시작일 수정 |
| tb_pj_project | end_de | 종료일 수정 |
| tb_pj_project | mdfcn_dt | 수정 일시 갱신 |
| tb_pj_settings_history | hist_id | PK, UUID 자동 생성 |
| tb_pj_settings_history | prjct_id | 프로젝트 FK |
| tb_pj_settings_history | chg_mber_id | 변경자 FK |
| tb_pj_settings_history | chg_item_nm | 변경 항목명 |
| tb_pj_settings_history | bfr_val_cn | 변경 전 값 |
| tb_pj_settings_history | aftr_val_cn | 변경 후 값 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 프로젝트명 공백 | - | 프로젝트명을 입력해 주세요 |
| 종료일 오류 | - | 종료일은 시작일 이후여야 합니다 |
| 권한 없음 | 403 | 접근 권한이 없습니다 |
| 서버 오류 | 500 | 저장 중 오류가 발생했습니다 |

### AR-00034 AI설정 탭 (FORM)

### 영역: [AR-00034] AI설정 탭

**유형:** FORM

**UI 구조**
```
+──────────────────────────────────────────+
| ## AI API 키                             |
| 프로바이더  키                  액션      |
| Claude API  sk-****1234  [수정] [삭제]   |
|                          [+ 키 등록]    |
|                                          |
| ## AI 호출 방식                          |
| ( ) Claude API 직접 호출                 |
| ( ) Claude Code 큐 기반                  |
|                          [저장]          |
+──────────────────────────────────────────+
```

**구성 항목**
| 항목명 | UI 타입 | 설명 | 기본값 |
|:-------|:--------|:-----|:-------|
| 프로바이더 | text | AI 프로바이더명 (Claude 등) | - |
| API 키 | text | 등록 후 마스킹 표시 (sk-****1234) | - |
| 수정 | button (secondary) | 키 수정 인라인 처리 | - |
| 삭제 | button (danger) | 키 삭제, 인라인 확인 후 처리 | - |
| 키 등록 | button (secondary) | 신규 키 등록 인라인 폼 표시 | - |
| AI 호출 방식 | radio | Claude API 직접 호출 / Claude Code 큐 기반 | 직접 호출 |
| 저장 | button (primary) | 호출 방식 저장 | - |

#### FID-00077 AI설정 조회

#### 기능: [FID-00077] AI설정 조회

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | SELECT |
| **API** | `GET /api/projects/{projectId}/settings/ai` |
| **트리거** | AI설정 탭 진입 시 자동 실행 |

**Output**
| 필드 | 타입 | 설명 |
|:-----|:-----|:-----|
| apiKeys | array | 등록된 API 키 목록 (keyId, provider, maskedKey) |
| callMethod | string | 현재 AI 호출 방식 (DIRECT/QUEUE) |

**처리 로직**
```
1. AI 설정 정보 API 호출
2. 서버 처리:
   a. tb_pj_project_api_key에서 prjct_id로 조회 (api_key_id, provdr_nm, mask_key_val)
   b. tb_pj_project_settings에서 ai_call_mthd_code 조회
3. API 키 목록 마스킹 처리 후 바인딩 (mask_key_val 사용)
4. 현재 호출 방식 라디오 버튼 선택 표시
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project_api_key | api_key_id | 키 PK |
| tb_pj_project_api_key | provdr_nm | 프로바이더명 |
| tb_pj_project_api_key | mask_key_val | 마스킹 키 표시 |
| tb_pj_project_settings | ai_call_mthd_code | 현재 호출 방식 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 권한 없음 | 403 | 접근 권한이 없습니다 |

#### FID-00078 AI API 키 등록

#### 기능: [FID-00078] AI API 키 등록

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | INSERT |
| **API** | `POST /api/projects/{projectId}/settings/api-keys` |
| **트리거** | [+ 키 등록] 버튼 클릭 후 입력 완료 시 |

**Input**
| 파라미터 | 타입 | 필수 | 설명 |
|:---------|:-----|:-----|:-----|
| provider | string | Y | AI 프로바이더명 |
| apiKey | string | Y | API 키 원문 (서버에서 암호화 저장) |

**처리 로직**
```
1. 인라인 등록 폼 표시
2. 키 입력 후 등록 API 호출
3. 서버 처리:
   a. tb_pj_project_api_key INSERT:
      - api_key_id: UUID 자동 생성
      - prjct_id: 현재 프로젝트
      - provdr_nm: 프로바이더명
      - encpt_key_val: API 키 AES 암호화 저장
      - mask_key_val: 마스킹 생성 (예: sk-****1234)
   b. tb_pj_settings_history INSERT (chg_item_nm = 'API 키 등록', aftr_val_cn = mask_key_val)
4. 성공 시 목록 갱신 (마스킹 처리된 키 표시)
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project_api_key | api_key_id | PK, UUID 자동 생성 |
| tb_pj_project_api_key | prjct_id | 프로젝트 FK |
| tb_pj_project_api_key | provdr_nm | 프로바이더명 |
| tb_pj_project_api_key | encpt_key_val | AES 암호화 키 저장 |
| tb_pj_project_api_key | mask_key_val | 마스킹 키 (sk-****1234) |
| tb_pj_settings_history | hist_id | 이력 PK |
| tb_pj_settings_history | chg_item_nm | 'API 키 등록' |
| tb_pj_settings_history | aftr_val_cn | 마스킹 키값 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 키 공백 | - | API 키를 입력해 주세요 |
| 서버 오류 | 500 | 키 등록 중 오류가 발생했습니다 |

#### FID-00079 AI API 키 수정

#### 기능: [FID-00079] AI API 키 수정

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | UPDATE |
| **API** | `PUT /api/projects/{projectId}/settings/api-keys/{keyId}` |
| **트리거** | [수정] 버튼 클릭 후 입력 완료 시 |

**Input**
| 파라미터 | 타입 | 필수 | 설명 |
|:---------|:-----|:-----|:-----|
| apiKey | string | Y | 새 API 키 원문 |

**처리 로직**
```
1. 인라인 수정 폼 표시 (기존 키는 공백으로 표시)
2. 새 키 입력 후 수정 API 호출
3. 서버 처리:
   a. tb_pj_project_api_key UPDATE (encpt_key_val = 새 키 AES 암호화, mask_key_val = 새 마스킹, mdfcn_dt = NOW())
   b. tb_pj_settings_history INSERT (chg_item_nm = 'API 키 수정', bfr_val_cn = 기존 mask_key_val, aftr_val_cn = 새 mask_key_val)
4. 성공 시 목록 갱신
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project_api_key | encpt_key_val | 새 키 AES 암호화 저장 |
| tb_pj_project_api_key | mask_key_val | 새 마스킹 갱신 |
| tb_pj_project_api_key | mdfcn_dt | 수정 일시 갱신 |
| tb_pj_settings_history | chg_item_nm | 'API 키 수정' |
| tb_pj_settings_history | bfr_val_cn | 기존 마스킹 |
| tb_pj_settings_history | aftr_val_cn | 새 마스킹 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 키 공백 | - | API 키를 입력해 주세요 |
| 서버 오류 | 500 | 키 수정 중 오류가 발생했습니다 |

#### FID-00080 AI API 키 삭제

#### 기능: [FID-00080] AI API 키 삭제

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | DELETE |
| **API** | `DELETE /api/projects/{projectId}/settings/api-keys/{keyId}` |
| **트리거** | [삭제] 버튼 클릭 시 |

**처리 로직**
```
1. 인라인 확인 텍스트 표시 ('삭제하시겠습니까? [확인] [아니오]')
2. 확인 클릭 시 삭제 API 호출
3. 서버 처리:
   a. tb_pj_project_api_key DELETE (해당 api_key_id)
   b. tb_pj_settings_history INSERT (chg_item_nm = 'API 키 삭제', bfr_val_cn = provdr_nm + mask_key_val)
4. 성공 시 목록에서 해당 행 제거
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project_api_key | api_key_id | 삭제 대상 PK |
| tb_pj_settings_history | chg_item_nm | 'API 키 삭제' |
| tb_pj_settings_history | bfr_val_cn | 삭제된 키 마스킹 정보 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 서버 오류 | 500 | 키 삭제 중 오류가 발생했습니다 |

#### FID-00081 AI 호출 방식 저장

#### 기능: [FID-00081] AI 호출 방식 저장

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | UPDATE |
| **API** | `PUT /api/projects/{projectId}/settings/ai` |
| **트리거** | [저장] 버튼 클릭 시 |

**Input**
| 파라미터 | 타입 | 필수 | 설명 |
|:---------|:-----|:-----|:-----|
| callMethod | string | Y | DIRECT(직접 호출) / QUEUE(큐 기반) |

**처리 로직**
```
1. 선택된 호출 방식 저장 API 호출
2. 서버 처리:
   a. tb_pj_project_settings UPDATE (ai_call_mthd_code = callMethod, mdfcn_dt = NOW())
   b. tb_pj_settings_history INSERT (chg_item_nm = 'AI 호출 방식', bfr_val_cn = 기존값, aftr_val_cn = 새값)
3. 성공 시 '저장되었습니다' 토스트 표시
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_project_settings | ai_call_mthd_code | 호출 방식 변경 (DIRECT/QUEUE) |
| tb_pj_project_settings | mdfcn_dt | 수정 일시 갱신 |
| tb_pj_settings_history | chg_item_nm | 'AI 호출 방식' |
| tb_pj_settings_history | bfr_val_cn | 변경 전 값 |
| tb_pj_settings_history | aftr_val_cn | 변경 후 값 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 권한 없음 | 403 | 접근 권한이 없습니다 |
| 서버 오류 | 500 | 저장 중 오류가 발생했습니다 |

### AR-00035 변경이력 탭 (GRID)

### 영역: [AR-00035] 변경이력 탭

**유형:** GRID

**UI 구조**
```
+──────────────────────────────────────────────────+
| 변경일시         | 변경자   | 항목   | 변경 내용  |
| 2024-01-01 12:00 | hong@... | API 키 | 등록 (마스킹) |
| 2024-01-01 11:00 | hong@... | 호출방식 | DIRECT→QUEUE |
+──────────────────────────────────────────────────+
```

**구성 항목**
| 항목명 | UI 타입 | 설명 | 기본값 |
|:-------|:--------|:-----|:-------|
| 변경일시 | datetime | 변경 발생 일시 | - |
| 변경자 | text | 변경을 수행한 사용자 이메일 | - |
| 항목 | text | 변경된 설정 항목명 | - |
| 변경 내용 | text | 변경 전→후 값 (API 키는 마스킹) | - |

#### FID-00082 변경이력 조회

#### 기능: [FID-00082] 변경이력 조회

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | SELECT |
| **API** | `GET /api/projects/{projectId}/settings/history` |
| **트리거** | 변경이력 탭 진입 시 자동 실행 |

**Output**
| 필드 | 타입 | 설명 |
|:-----|:-----|:-----|
| items | array | 변경 이력 목록 (changedAt, changerEmail, itemName, beforeValue, afterValue) |
| totalCount | number | 전체 건수 |

**처리 로직**
```
1. 설정 변경 이력 API 호출
2. 서버 처리:
   a. tb_pj_settings_history에서 prjct_id로 조회
   b. tb_cm_member JOIN → chg_mber_id로 변경자 email_addr 조회
   c. chg_dt 내림차순 정렬
3. API 키 관련 이력은 bfr_val_cn/aftr_val_cn에 이미 마스킹된 값 사용
```

**참조 테이블**
| 테이블 | 컬럼 | 용도 |
|:-------|:-----|:-----|
| tb_pj_settings_history | prjct_id | 프로젝트 필터 |
| tb_pj_settings_history | chg_mber_id | 변경자 FK |
| tb_pj_settings_history | chg_item_nm | 변경 항목명 |
| tb_pj_settings_history | bfr_val_cn | 변경 전 값 |
| tb_pj_settings_history | aftr_val_cn | 변경 후 값 |
| tb_pj_settings_history | chg_dt | 변경 일시 + 정렬 |
| tb_cm_member | email_addr | 변경자 이메일 표시 |

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 결과 0건 | 200 | '변경 이력이 없습니다' 안내 표시 |
| 권한 없음 | 403 | 접근 권한이 없습니다 |

---
