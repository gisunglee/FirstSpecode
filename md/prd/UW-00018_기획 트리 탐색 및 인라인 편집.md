# UW-00018 기획 트리 탐색 및 인라인 편집

> ## 1. 개요
| 항목 | 내용 |
|:-----|:-----|
| **단위업무ID** | UW-00018 |
| **단위업무명** | 기획 트리 탐색 및 인라인 편집 |
| **비즈니스 목적** | 과업(SFR), 요구사항(REQ), 사용자스토리(STR)의 계층 구조를 단일 화면에서 탐색하고 인라인으로 즉시 편집하여 기획 자산의 정합성을 확보한다. |
| **관련 요구사항** | RQ-00018, RQ-00014, RQ-00015, RQ-00016 |

## 2. 화면 목록
| 화면ID | 화면명 | URL | 유형 | 설명 |
|:-------|:-------|:----|:-----|:-----|
| PID-00039 | 기획 트리 | /projects/{projectId}/planning/tree | DETAIL | 좌측 트리 탐색기 + 우측 상세 편집 패널 (인라인 편집) |

## 3. 화면 흐름
~~~
[트리 진입] → 전체 데이터 로드 → 트리 노드 렌더링
  ├─ [노드 클릭] → 상세 데이터 Fetch → 우측 패널 활성화
  ├─ [+ 추가 클릭] → 선택 노드 하위에 신규 항목 생성(Draft) → 즉시 편집 모드
  └─ [저장 클릭] → 데이터 업데이트 + 변경 이력 생성 → 트리 상태 동기화
~~~

## 4. 권한 정의
| 기능 | VIEWER | PM/DESIGNER | ADMIN | OWNER |
|:-----|:-------|:-----------|:-----|:-----|
| 트리 조회 | ✅ | ✅ | ✅ | ✅ |
| 인라인 편집/추가 | ❌ | ✅ | ✅ | ✅ |
| 항목 삭제 | ❌ | ❌ | ✅ | ✅ |

## 5. 상태 정의
- **요구사항 진행 상태**: REGISTERED → ANALYZING → ANALYSIS_DONE → REVISING → REVISION_DONE
- **버전 관리**: 저장 시 tb_rq_requirement_history에 INTERNAL 버전으로 자동 누적

## 6. 참조 테이블
- <TABLE_SCRIPT:tb_rq_task>
- <TABLE_SCRIPT:tb_rq_requirement>
- <TABLE_SCRIPT:tb_rq_requirement_history>
- <TABLE_SCRIPT:tb_rq_user_story>
- <TABLE_SCRIPT:tb_rq_acceptance_criteria>

총 화면 1개 · 영역 2개 · 기능 7개

---

## PID-00039 기획 트리

### AR-00057 트리 탐색 패널 (FULL_SCREEN)

### 영역: [AR-00057] 트리 탐색 패널

**유형:** FULL_SCREEN

**UI 구조**
~~~
+──────────────────────────────────────────────────────────+
| [🔍 검색어 입력...]                 [+ 과업추가]         |
|──────────────────────────────────────────────────────────|
| ▼ 📁 [SFR-001] 회원 관리 [12]            [+] [x]         |
|   ▼ 📝 [REQ-001] 소셜 로그인 ● HIGH [✓]  [+] [x]         |
|     - 👤 [STR-001] 구글 연동             [+] [x]         |
+──────────────────────────────────────────────────────────+
~~~

**구성 항목**
| 항목명 | UI 타입 | 관련 컬럼 | 설명 |
|:-------|:--------|:----------|:-----|
| 트리 노드 명칭 | text | display_id + name | SFR-001 회원관리 형태로 표시 |
| 상태 아이콘 | icon | req_sttus_code | 요구사항의 현재 단계별 아이콘 표시 |
| 추가 버튼 | button | - | 노드 호버 시 노출, 하위 레벨 항목 생성 |
| 삭제 버튼 | button | - | 노드 호버 시 노출, 항목 삭제 |

#### FID-00126 트리 전체 로드

#### 기능: [FID-00126] 트리 전체 로드

**기술 매핑**
| 구분 | 테이블 | 주요 컬럼 |
|:-----|:-------|:----------|
| 조회 | <TABLE_SCRIPT:tb_rq_task> | task_id, task_display_id, task_nm |
| 조회 | <TABLE_SCRIPT:tb_rq_requirement> | req_id, req_display_id, req_nm, priort_code, req_sttus_code |
| 조회 | <TABLE_SCRIPT:tb_rq_user_story> | story_id, story_display_id, story_nm |

**처리 로직**
~~~
1. 프로젝트 내 모든 기획 데이터를 계층 구조로 조회(Recursive Query 또는 일괄 조회 후 가공)
2. sort_ordr 기준 정렬 유지
~~~

#### FID-00127 트리 키워드 검색

#### 기능: [신규] 트리 키워드 검색

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | SELECT |
| **트리거** | 키워드 입력 시 실시간 |

**처리 로직**
```
1. 입력 키워드로 과업명·요구사항명·스토리명 부분 일치 검색
2. 매칭 항목 하이라이트 표시
3. 매칭 항목 상위 노드 자동 펼침
4. 키워드 초기화 시 트리 원래 상태 복귀
```

#### FID-00132 트리 항목 추가

#### 기능: [신규] 트리 항목 추가

**기술 매핑**
| 구분 | 테이블 | 주요 컬럼 |
|:-----|:-------|:----------|
| 생성 | <TABLE_SCRIPT:tb_rq_requirement> | prjct_id, task_id, req_display_id, req_nm, sort_ordr |
| 생성 | <TABLE_SCRIPT:tb_rq_user_story> | prjct_id, req_id, story_display_id, story_nm, sort_ordr |

**처리 로직**
~~~
1. 선택된 부모 노드에 맞는 하위 테이블에 INSERT
2. display_id는 해당 프로젝트 내 해당 유형의 MAX+1 자동 채번
3. 생성 즉시 트리에 추가하고 우측 패널을 수정 모드로 전환
~~~

#### FID-00128 드래그앤드롭 순서 변경 및 이동

#### 기능: [신규] 드래그앤드롭 순서 변경 및 이동

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | UPDATE |
| **API** | 순서 변경: `PUT /api/projects/{projectId}/requirements/sort` / 과업 간 이동: `PUT /api/projects/{projectId}/requirements/{requirementId}` |
| **트리거** | 드래그앤드롭 완료 시 |

**처리 로직**
```
1. 동일 과업 내 드롭 → 순서 변경 API 호출
2. 다른 과업으로 드롭 → 과업 간 이동 API 호출
3. 사용자스토리 → 다른 요구사항으로 이동 가능
4. 성공 시 트리 즉시 갱신
```

**에러 처리**
| 상황 | HTTP | 메시지 |
|:-----|:-----|:-------|
| 서버 오류 | 500 | 이동 중 오류가 발생했습니다 |

#### FID-00129 트리 항목 삭제

#### 기능: [신규] 트리 항목 삭제

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | DELETE |
| **API** | 과업: `DELETE /api/projects/{projectId}/tasks/{taskId}` / 요구사항: `DELETE /api/projects/{projectId}/requirements/{requirementId}` / 스토리: `DELETE /api/projects/{projectId}/user-stories/{storyId}` |
| **트리거** | [삭제] 버튼 클릭 |

**처리 로직**
```
1. 항목 레벨별 경고 메시지 표시
   - 과업: '과업과 하위 요구사항·스토리가 모두 삭제됩니다'
   - 요구사항: '요구사항과 하위 스토리가 모두 삭제됩니다'
   - 스토리: '사용자스토리와 인수기준이 삭제됩니다'
2. 확인 클릭 시 해당 API 호출
3. 성공 시 트리에서 항목 즉시 제거, 우측 패널 초기화
```

### AR-00058 상세 편집 패널 (FORM)

### 영역: [AR-00058] 상세 편집 패널

**유형:** FORM

**구성 항목**
| 항목명 | UI 타입 | 관련 컬럼 | 설명 |
|:-------|:--------|:----------|:-----|
| 명칭 | text input | req_nm / story_nm | 항목의 핵심 제목 |
| 상태 | select | req_sttus_code | [v3] 진행 상태 선택 |
| 담당자 | select | asign_mber_id | [v3] 업무 담당자 할당 |
| 인수기준 리스트 | editable list | tb_rq_acceptance_criteria | [v3] STR 선택 시 노출, Given/When/Then 편집 |
| 버전 코멘트 | text area | vrsn_coment_cn | [v3] 저장 시 히스토리에 남길 변경 사유 |

**UI 구조**
~~~
+──────────────────────────────────────────────────────────+
| 상태: [진행중 ▼] 담당자: [홍길동 ▼]                        |
|──────────────────────────────────────────────────────────|
| [항목 명칭 입력 필드]                                    |
| [명세 내용 에디터 영역]                                  |
|──────────────────────────────────────────────────────────|
| 버전 코멘트: [변경 사유를 입력하세요...]    [취소] [저장]|
+──────────────────────────────────────────────────────────+
~~~

#### FID-00130 항목 상세 조회

#### 기능: [신규] 항목 상세 조회

| 항목 | 내용 |
|:-----|:-----|
| **기능유형** | SELECT |
| **API** | 과업: `GET /api/projects/{projectId}/tasks/{taskId}` / 요구사항: `GET /api/projects/{projectId}/requirements/{requirementId}` / 스토리: `GET /api/projects/{projectId}/user-stories/{storyId}` |
| **트리거** | 트리 항목 클릭 시 |

**처리 로직**
```
1. 클릭한 항목 레벨 판별
2. 해당 API 호출 → 우측 패널 읽기 모드로 표시
3. 요구사항 선택 시 → 과업 요약 카드 함께 표시
4. 스토리 선택 시 → 과업 요약 카드 + 요구사항 요약 카드 함께 표시
```

#### FID-00131 항목 저장

#### 기능: [FID-00131] 항목 저장

**기술 매핑**
| 구분 | 테이블 | 주요 컬럼 |
|:-----|:-------|:----------|
| 수정 | <TABLE_SCRIPT:tb_rq_requirement> | req_nm, spec_cn, req_sttus_code, asign_mber_id |
| 이력 | <TABLE_SCRIPT:tb_rq_requirement_history> | vrsn_no, vrsn_coment_cn, spec_cn, chg_mber_id |
| 수정 | <TABLE_SCRIPT:tb_rq_acceptance_criteria> | ac_id, given_cn, when_cn, then_cn |

**처리 로직**
~~~
1. 변경된 필드 업데이트 (Update)
2. [v3] vrsn_coment_cn을 포함하여 tb_rq_requirement_history에 신규 스냅샷 생성
3. 인수기준(AC) 변경 사항 일괄 저장 (Upsert)
~~~

---
