/**
 * test-policy.ts — MCP 테스트 명세 정책 (서버에서 내려보내는 행동 규칙)
 *
 * 역할:
 *   - MCP initialize instructions 에 덧붙일 테스트 명세 작성 규칙 제공
 *   - 테스트 쓰기 도구 description 에 붙일 짧은 정책 포인터 제공
 *
 * 왜 서버에 두는가:
 *   design-policy.ts 와 같은 이유다. 고객은 각자 로컬에서 AI 클라이언트를 쓰므로
 *   고객 저장소의 커맨드 파일이나 CLAUDE.md 는 고객이 고칠 수 있다. 테스트 케이스의
 *   세분화 기준이 사람마다 달라지는 것("누구는 버튼 단위, 누구는 화면 단위")을 막으려면
 *   기준이 서버에 있어야 모든 고객·모든 작성자에게 동일하게 적용된다.
 *
 * 왜 instructions 와 포인터로 나누는가:
 *   instructions 는 initialize 때 한 번만 주입되어 대화가 길어지면 묻힌다.
 *   그래서 도구 description 에도 짧은 포인터를 붙인다. 반대로 긴 본문을 도구마다
 *   복사하면 tools/list 응답이 비대해져 규칙이 오히려 희석된다 — design-policy.ts 와
 *   동일한 2단 구성을 따른다.
 */

// ─── MCP initialize instructions — 테스트 절 ────────────────────
// SPECODE_MCP_INSTRUCTIONS 본문 뒤에 이어 붙는다.
export const SPECODE_TEST_INSTRUCTIONS = `
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[테스트 1] 케이스 세분화 — 버튼 단위로 쪼개지 말 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
테스트 케이스의 단위는 "한 번의 판정으로 합/불이 갈리는 사용자 행위 묶음"이다.
UI 요소 목록이 아니다.

기본값 — 기능(Function) 1개당 정상 1건 + 예외 0~2건.
  · "저장 버튼 클릭", "취소 버튼 클릭", "입력란에 값 입력"을 각각 케이스로
    만들지 않는다. 이런 것들은 하나의 행위 묶음 안에 들어간다.
  · 반대로 화면 하나를 케이스 1건으로 뭉치지도 않는다. 깨졌을 때 어디가
    깨졌는지 알 수 없다.

예외 케이스는 "실패해야 정상인 경로"만 만든다 — 필수값 누락, 권한 없음,
중복 등록, 한도 초과 같은 것. 일어날 수 없는 조합을 지어내지 않는다.

한 화면의 FUNCTIONAL 케이스가 8건을 넘을 것 같으면, 등록하기 전에 케이스
목록(시나리오 제목만)을 사용자에게 먼저 제시하고 동의를 받는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[테스트 2] 공통 점검(CHECKLIST)은 창작 금지 — 마스터에서만 가져올 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ctgryCode="CHECKLIST" 케이스를 직접 지어내지 않는다.
"오타가 없는가", "권한에 따라 버튼이 노출되는가" 같은 공통 점검 항목은
list_check_masters 로 조회해 import_check_masters 로 복사한다.

이유 — 공통 점검을 매번 새로 쓰면 화면마다 문장이 미묘하게 달라진다.
마스터에서만 가져오면 프로젝트 전체가 같은 문장을 쓰게 된다.

해당 화면에 없는 기능의 점검 항목(예: 검색이 없는 화면의 검색 점검)은
가져오되 applicableYn="N" (해당없음)으로 표시한다. 빼지 않는다 —
"검토했고 해당 없음"과 "빠뜨림"은 다르다.

직접 작성하는 것은 ctgryCode="FUNCTIONAL" 케이스뿐이다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[테스트 3] 설계에 없는 것을 테스트하지 말 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
케이스는 SPECODE 에 등록된 설계(화면·영역·기능)에서 나와야 한다.
get_design_tree 로 대상 단위업무의 설계를 먼저 읽고, 거기 있는 기능에
대해서만 케이스를 쓴다.

설계에 없는데 테스트가 필요해 보이는 것이 있으면, 케이스를 지어내지 말고
사용자에게 "설계에 이 기능이 없는데 테스트 대상인지" 묻는다. 대개는 설계가
빠진 것이므로 설계를 먼저 보완하는 게 맞다.

예상결과(expectedCn)는 설계에 적힌 동작을 그대로 쓴다. 설계에 없는 동작을
추측해서 적으면 그 케이스는 영원히 불합격한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[테스트 4] 문장 작성 — 누가 읽어도 같은 판정이 나오게
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
시나리오(scenarioCn) — "무엇을 하는가"를 행위로 쓴다.
  ○ "필수값(이메일)을 비운 채 [저장]을 누른다"
  ✗ "이메일 검증"          (무엇을 하는지 알 수 없음)
  ✗ "이메일이 잘 되는지"    (판정 불가)

예상결과(expectedCn) — "무엇을 보고 합격이라 판단하는가"를 관측 가능하게 쓴다.
  ○ "저장되지 않고 이메일 입력란 아래에 '필수 항목입니다' 가 표시된다"
  ✗ "에러가 난다"          (어떤 에러인지 모름)
  ✗ "정상 동작한다"        (무엇이 정상인지 모름)

전제조건(preconditionCn)은 그 케이스를 재현하는 데 꼭 필요할 때만 쓴다.
"로그인 상태" 처럼 모든 케이스에 공통인 것은 반복해 적지 않는다.

구분(grpNm)은 FUNCTIONAL 에만 쓰고, 화면 안의 기능 묶음 이름을 넣는다
(예: "회원 등록", "권한 변경"). CHECKLIST 에는 넣지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[테스트 5] 명세서까지만 — 결과는 사람이 쓴다
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MCP 로 할 수 있는 것은 "무엇을 테스트할지" 명세를 만드는 것까지다.
회차 생성, 합부 판정(PASS/FAIL), 결함 등록, 증적 첨부는 도구가 없다.

실제로 테스트를 수행하지 않고 결과를 기록하면 그 기록 전체의 신뢰도가
사라진다. 사용자가 "결과도 채워줘" 라고 요청하면, 결과 입력은 웹 화면에서
테스트를 수행한 사람이 직접 해야 한다고 안내한다.

또한 테스트 케이스는 MCP 로 삭제할 수 없다. upsert_test_cases 는 추가와
수정만 한다. 케이스를 지워야 하면 웹 화면에서 사람이 지운다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[테스트 6] 이미 돌고 있는 회차를 건드릴 때는 먼저 말할 것
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
get_test_spec 응답의 rounds(회차 목록)와 openRoundNos(진행중 회차 번호)를
케이스를 고치기 전에 확인한다. 목록의 마지막 항목이 최신 회차다.

진행중 회차가 있는데 케이스를 추가하면:
  · 추가한 케이스가 그 회차의 결과 입력 목록에 즉시 나타난다(미판정 상태).
  · 테스터가 이미 "다 끝냈다" 고 생각하던 회차에 할 일이 늘어난다.

그래서 진행중 회차가 있으면 추가하기 전에 사용자에게 알린다 —
"지금 2차가 진행중입니다. 케이스 3건을 추가하면 2차에 미판정 3건이
생깁니다. 2차에 포함할까요, 아니면 2차를 끝낸 뒤에 넣을까요?"

이미 완료(DONE)된 회차에는 아무 영향이 없다. 그때 그 범위로 나온 결과는
확정 기록이므로 소급해서 바뀌지 않는다.

케이스 수정도 마찬가지다. 진행중 회차에서 이미 판정된 케이스의 시나리오나
예상결과를 고치면, 그 판정은 "고치기 전 문장 기준" 이 되어 의미가 어긋난다.
판정이 끝난 케이스의 본문을 고쳐야 하면 사용자에게 먼저 알린다.
`.trim();

// ─── 도구 description 에 붙이는 짧은 포인터 ──────────────────────

/** 진행중 회차 영향 — 케이스를 늘리는 도구에 붙인다 */
export const TEST_OPEN_ROUND_POINTER =
  "[진행중 회차] 케이스를 추가하면 진행중 회차의 결과 입력 목록에 미판정 항목으로 즉시 " +
  "나타납니다(완료된 회차는 영향 없음). get_test_spec 의 openRoundNos 로 진행중 회차를 " +
  "먼저 확인하고, 진행중 회차가 있으면 추가 전에 사용자에게 알리세요. 응답의 " +
  "openRoundResultsAdded 가 실제로 열린 결과행 수이니 보고에 포함하세요.";

/** 테스트 쓰기 도구 공통 — 합의·보고 의무 + 결과 미지원 */
export const TEST_WRITE_POLICY_POINTER =
  "[정책] 등록 전 사용자 허락 필수(userAgreement). 등록·수정한 내용은 표시 ID와 함께 " +
  "반드시 사용자에게 알리세요(생략 금지). 테스트 결과(회차·합부·결함)는 MCP로 다룰 수 " +
  "없습니다 — 명세서 작성까지만 가능하며, 결과는 테스트를 수행한 사람이 웹에서 입력합니다.";

/** 케이스 작성 도구 전용 — 과분할이 가장 자주 발생하는 지점 */
export const TEST_CASE_GRANULARITY_POINTER =
  "[케이스 단위] 기능 1개당 정상 1건 + 예외 0~2건이 기본입니다. 버튼·입력란마다 케이스를 " +
  "만들지 마세요. 케이스 단위는 '한 번의 판정으로 합/불이 갈리는 사용자 행위 묶음'입니다. " +
  "한 화면의 FUNCTIONAL 케이스가 8건을 넘을 것 같으면 등록 전에 케이스 목록을 사용자에게 " +
  "먼저 제시하세요. CHECKLIST 케이스는 직접 지어내지 말고 import_check_masters 로만 " +
  "가져오세요. 예상결과는 '에러가 난다' 같은 판정 불가 문장 대신 화면에서 관측 가능한 " +
  "내용으로 쓰세요.";

/** 삭제 불가 — 웹 PUT 과 동작이 다르다는 점을 명시 */
export const TEST_CASE_NO_DELETE_POINTER =
  "[삭제 불가] 이 도구는 추가·수정만 합니다. 보내지 않은 기존 케이스는 그대로 유지됩니다 " +
  "(웹 화면의 일괄 저장과 동작이 다릅니다). 케이스 삭제는 웹 화면에서만 가능합니다.";
